# Changelog

## 1.1.5

- Fix: asset dictionary tree showed no tags. Node entries are now matched on the
  logical `entryId` (shared across bindings) instead of the per-binding `id`,
  which the DataCatalog binding model made distinct.

## 1.0.0 (Unreleased)

Initial release.
